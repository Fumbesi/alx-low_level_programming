# Makefile for building the 'school' executable

# Rule to build the executable
all: school

# Compile and link the 'school' executable
school: main.c utils.c
    gcc -o school main.c utils.c

# Clean rule to remove generated files
clean:
    rm -f school

# Phony targets (targets that aren't real files)
.PHONY: all clean
